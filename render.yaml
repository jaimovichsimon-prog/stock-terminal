services:
  - type: web
    name: stock-terminal
    runtime: python
    rootDir: stock-terminal
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
